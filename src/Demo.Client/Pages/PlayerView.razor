@page "/player-view"
@rendermode InteractiveWebAssembly

<h3>MJPEG Player (iframe)</h3>
<p>The player below runs in a separate WASM runtime with threading enabled:</p>

<iframe src="/player/" style="width:100%; height:620px; border:2px solid #444; border-radius:4px;" allow="cross-origin-isolated"></iframe>
